# Supabase client
supabase

# Voyage AI for embeddings
voyageai

# HTTP requests for image download
requests

# Image handling
Pillow

# COCO RLE mask decoding for SAM3
pycocotools

# UI for testing
streamlit

# Environment variables
python-dotenv

# SDK for inference
inference-sdk

#used for dinov2 embeddings, not used for voyage embeddings, hence discontinued, use if you want to generate dinov2 embeddings
# PyTorch and transformers
# torch
# torchvision --index-url https://download.pytorch.org/whl/cpu
# transformers

# FastAPI and web server
fastapi
uvicorn[standard]

# Database drivers
psycopg2-binary
asyncpg

# Data validation (used by FastAPI)
pydantic

# Numerical computing (for SAM3 segments)
numpy
